import styled from "@emotion/styled";

export const Top = () => {
  return (
    <SContainer>
      <h2>This is top page.</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`